#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>


int main()
{
    // extern char **environ;
    // for(int i = 0; environ[i] != NULL; i++)
    // {
    //     printf("environ[%2d]: %s\n",i,environ[i]);
    // }
    sleep(20);
    // char *path = getenv("PATH");
    // printf("path: %s\n",path);
    /* puts */
    // puts("hello");
    // puts("goodmoring");
    /* fputs */
    // fputs("hello\n",stdout);
    // fputs("good",stdout);
    // fputs("moning\n",stdout);
    exit(0);
}